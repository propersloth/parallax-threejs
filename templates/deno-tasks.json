{
  "checkpoint": "deno run --allow-all scripts/checkpoint.ts",
  "sweep": "deno run --allow-all scripts/sweep-param.ts",
  "diff-checkpoints": "deno run --allow-all scripts/diff-checkpoints.ts",
  "replay": "deno run --allow-all scripts/replay.ts",
  "visual:run": "deno run --allow-all test/visual/run.ts run",
  "visual:accept": "deno run --allow-read --allow-write test/visual/run.ts accept"
}
